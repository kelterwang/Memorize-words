# 淇澳背单词：重启与恢复

## 关机与重新运行

本项目保存在 `/Users/chaoyongwang/codex/002 背单词`。

关机前正常关闭模拟器，再关闭系统。进程本身不会跨关机保留，代码、安装包、模拟器磁盘及学习记录保存在硬盘上。

重新开机后，进入项目文件夹，双击 `启动淇澳背单词.command`。脚本会启动现有模拟器并打开 App，不需要重新导入词库，不会卸载 App 或清空数据。冷启动可能需要一两分钟。如果 macOS 阻止双击，可在终端执行：

```bash
cd '/Users/chaoyongwang/codex/002 背单词'
bash './启动淇澳背单词.command'
```

## 保存位置

- 源代码、设计方案、技术方案和需求文档：项目目录；代码修改由 Git 记录。
- 安卓手机安装包：`安装包/淇澳背单词-1.0.apk`，独立于构建缓存；测试签名，支持 Android 8.0 及以上。
- Java 与 Android SDK：`.local-runtime/`，不再依赖 `/private/tmp` 临时目录。
- 模拟器及学习数据：`/Users/chaoyongwang/.android/avd/MorningWords_API_36.avd`。保留整个目录，勿使用 Wipe Data 或卸载 App。
- 关机前备份：`backups/2026-09-03-shutdown/`，包括模拟器、源码归档、Git 历史、安装包及调试签名密钥。
- 调试签名密钥原件：`/Users/chaoyongwang/.android/debug.keystore`。保留它才能继续覆盖安装同一测试版。

备份只在本机，不能防止硬盘损坏；如需额外保障，请将项目目录和模拟器备份复制到外置硬盘。签名密钥和模拟器备份包含私密数据，请勿公开上传。

## 构建与验证

以下构建命令在 Bash 中执行（macOS 默认 Zsh，请先执行第一行 `bash`）：

```bash
bash
cd '/Users/chaoyongwang/codex/002 背单词'
source scripts/android-env.sh
bash './启动淇澳背单词.command' --check
python3 -m unittest discover -s tests -v
./gradlew --offline testDebugUnitTest lintDebug assembleDebug
```

设备测试 `./gradlew --offline connectedDebugAndroidTest` 可能重新安装或卸载被测 App，必须使用专用测试模拟器，不能直接对保存日常学习数据的模拟器运行。需要使用当前模拟器时，应先关闭它并完整备份，测试后恢复原始模拟器数据。

构建输出为 `app/build/outputs/apk/debug/app-debug.apk`。以后修改代码后，需重新构建并使用 `adb -s emulator-5554 install -r app/build/outputs/apk/debug/app-debug.apk` 更新模拟器；启动脚本不会覆盖已安装版本。

## 异常恢复

启动日志在 `.local-runtime/emulator.log`。如果提示端口占用，关闭占用端口 5554 的其他模拟器后重试。不要为了启动而删除模拟器数据。

如模拟器损坏，先关闭所有模拟器，把当前 `.android/avd` 目录另行备份，再从关机备份的 `avd` 目录恢复对应的 `MorningWords_API_36.ini` 和 `MorningWords_API_36.avd`。恢复会回到备份时的学习进度，因此不要直接覆盖仍有新数据的目录。

`source.tar.gz` 保存了包括未提交需求文档在内的源文件，`repository.bundle` 保存 Git 历史。需要恢复时先解压到新的空目录；不要直接覆盖当前项目。备份中的 `SHA256SUMS` 可用 `shasum -a 256 -c SHA256SUMS` 检查归档和安装包。
