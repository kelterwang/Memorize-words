# 离线发音

## 使用

安装 v1.2.0 APK，在“我的 → 选择离线语音包（ZIP）”中选择配套 `Kokoro-English-v1.0.zip`。导入成功后自动选择 Kokoro，可切换美音 Heart / 英音 Emma 并试听。导入需要约 200 MB 可用空间；导入和校验过程中请保持 App 打开。取消文件选择不改变现有设置；失败不会损坏已安装包。

手机自带发音仍是默认选项。两种发音均为 1 倍速。手机引擎优先选用指定口音的离线声音；只有联网声音时明确显示需要联网；缺少指定口音则提示安装，不会静默播放其他口音。

Kokoro 引擎随 App 安装，模型文件单独导入，并保存在不参与云备份的内部目录。美音采用 speaker ID 3（af_heart）及美音词典，英音采用 ID 21（bf_emma）及英音词典。页面离开、进入后台或再次点击发音时停止前次播放；生成在后台线程执行。首次发音需加载模型，速度取决于手机性能。

当前只接受配套 v1.0 ZIP，以内置 SHA-256 清单逐文件校验，不接受任意同名 ONNX 模型或其他网站语音包。重新安装或清除 App 数据后需再次导入；正常覆盖升级保留语音包及偏好。ZIP 可与 APK 一起分发，不需要账号或 API 密钥。

## 可复现的依赖与语音包

- 引擎：sherpa-onnx 1.13.7，`app/libs/sherpa-onnx-1.13.7.aar`，来自 [官方发布](https://github.com/k2-fsa/sherpa-onnx/releases/download/v1.13.7/sherpa-onnx-static-link-onnxruntime-1.13.7.aar)。
- 模型：[kokoro-int8-multi-lang-v1_0.tar.bz2](https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-int8-multi-lang-v1_0.tar.bz2)。
- 取得并解压模型后，执行 `python3 scripts/package_kokoro.py /path/to/kokoro-int8-multi-lang-v1_0`，生成 ZIP 与 App 资源中的文件校验清单。生成器只收录英文所需模型、双口音词典、音色、音素数据及许可文件。
- [官方声音映射](https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/kokoro.html)。

依赖许可及来源见 `third_party/README.md`。模型更新需要重新核对声音映射、生成清单并重新构建 App。

## 验证

使用专用测试模拟器，避免覆盖用户学习数据。将 ZIP 推送至 `/data/local/tmp/Kokoro-English-v1.0.zip` 后运行：

```bash
source scripts/android-env.sh
ANDROID_SERIAL=emulator-5556 ./gradlew --offline testDebugUnitTest lintDebug assembleDebug connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.kokoroPack=/data/local/tmp/Kokoro-English-v1.0.zip
python3 -m unittest discover -s tests
```

设备测试会真实导入语音包、分别用两种口音生成有声音的 24 kHz WAV，并确认 App 没有联网权限。单元测试覆盖 ZIP 文件缺失、损坏、路径越界和正常解压；界面测试覆盖来源/口音选择与持久化。
