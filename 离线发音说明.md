# 离线发音

## 使用

安装 v1.2.2 完整版 APK，第一次打开 App 会在后台自动解包并校验内置 Kokoro，无需单独 ZIP，也无需联网。首次准备约需 200 MB 可用空间；“我的”中会显示准备进度和失败重试按钮。已有完整语音包的升级安装直接复用。

手机自带发音仍是默认选项。两种发音均为 1 倍速。手机引擎优先选择所选口音；无法提供该口音时尝试其他可用英文声音，并显示实际口音。兼容只提供 setLanguage 的旧式引擎；初始化失败、无英文服务或播放失败时自动使用内置 Kokoro，界面明确显示回退原因，不会将 Kokoro 冒充手机声音。

Kokoro 引擎随 App 安装，模型文件单独导入，并保存在不参与云备份的内部目录。美音采用 speaker ID 3（af_heart）及美音词典，英音采用 ID 21（bf_emma）及英音词典。页面离开、进入后台或再次点击发音时停止前次播放；生成在后台线程执行。首次发音需加载模型，速度取决于手机性能。

可选的手动更换入口只接受配套 v1.0 ZIP，以内置 SHA-256 清单逐文件校验，不接受任意同名 ONNX 模型或其他网站语音包。重新安装或清除 App 数据后会自动重新准备内置包；正常覆盖升级保留语音包及偏好。不需要账号或 API 密钥。

## 可复现的依赖与语音包

- 引擎：sherpa-onnx 1.13.7，`app/libs/sherpa-onnx-1.13.7.aar`，来自 [官方发布](https://github.com/k2-fsa/sherpa-onnx/releases/download/v1.13.7/sherpa-onnx-static-link-onnxruntime-1.13.7.aar)。
- 模型：[kokoro-int8-multi-lang-v1_0.tar.bz2](https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-int8-multi-lang-v1_0.tar.bz2)。
- 取得并解压模型后，执行 `python3 scripts/package_kokoro.py /path/to/kokoro-int8-multi-lang-v1_0`，生成 ZIP、内置资源分片与 App 资源中的文件校验清单。生成器只收录英文所需模型、双口音词典、音色、音素数据及许可文件。
- [官方声音映射](https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/kokoro.html)。

依赖许可及来源见 `third_party/README.md`。模型更新需要重新核对声音映射、生成清单并重新构建 App。

## 验证

使用专用测试模拟器，避免覆盖用户学习数据。直接运行，无需推送外部模型：

```bash
source scripts/android-env.sh
ANDROID_SERIAL=emulator-5556 ./gradlew --offline testDebugUnitTest lintDebug assembleDebug connectedDebugAndroidTest
python3 -m unittest discover -s tests
```

设备测试会只使用 APK 内置资源自动准备语音包、分别用两种口音生成有声音的 24 kHz WAV，并确认 App 没有联网权限。单元测试覆盖 ZIP 文件缺失、损坏、路径越界和正常解压；界面测试覆盖来源/口音选择与持久化。
