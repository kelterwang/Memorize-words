# 离线发音

## 使用

安装 v1.2.5 完整版 APK，第一次打开 App 会在后台自动解包并校验内置 Kokoro，无需单独 ZIP，也无需联网。首次准备约需 400 MB 可用空间；“我的”中会显示准备进度和失败重试按钮。从旧版升级会自动准备 FP32 模型，成功后移除旧 int8 模型，保留学习记录和偏好。

手机自带发音仍是默认选项。两种发音均为 1 倍速。v1.2.4 会核对手机引擎实际返回的口音；无法确认所选口音时，明确改用内置 Kokoro 的对应口音，不会使用同一个通用英文声音代替两种口音。仅提供 setLanguage 的旧式引擎也必须返回匹配的实际口音才会使用。

Kokoro 改用 PCM16 直接输出，避免依赖手机对临时 WAV 文件的解码。设备测试覆盖英音首先播放、反复切换美音和英音，并等待音频帧真正播放完成。页面离开或重新点击发音时取消前次播放。

Kokoro 引擎随 App 安装，模型文件首次打开时自动准备，也支持手动替换导入，并保存在不参与云备份的内部目录。美音采用 speaker ID 3（af_heart）及美音词典，英音采用 ID 21（bf_emma）及英音词典。页面离开、进入后台或再次点击发音时停止前次播放；生成在后台线程执行。首次发音需加载模型，速度取决于手机性能。

可选的手动更换入口只接受配套 v1.0-FP32 ZIP，以内置 SHA-256 清单逐文件校验，不接受任意同名 ONNX 模型或其他网站语音包。重新安装或清除 App 数据后会自动重新准备内置包；正常覆盖升级保留学习数据及偏好，并按需升级模型。不需要账号或 API 密钥。

## v1.2.5 双口音播放

原有发音页面统一提供带喇叭图标的“美音”“英音”按钮，单词卡右上角固定显示当前口音。晨测、学生自测、家长考词、错词复测共用此组件，“我的”也提供双口音试听。自动朗读跟随“我的”中保存的口音与发音来源；手动对比试听不改变全局默认，切换单词或修改全局设置后恢复默认口音。切换时停止前次播放，只保留一个语音引擎；语音准备完成后再播放所选口音，离开页面或进入后台会停止。

## v1.2.4 单词静音修复

已在 Android ARM 模拟器复现旧 int8 模型搭配 Emma 英音时，`carsick`、`homesick`、`unique` 输出 NaN 音频。三个词均在词典中，文件校验通过。此版本改为 FP32 模型，保留 Heart / Emma 音色及 1 倍速；播放前拒绝非有限数值、空音频、静音及恒定信号，异常会显示错误而非当作成功播放。完整安装包体积随模型增加。设备回归覆盖上述单词、常见短词、两种口音以及旧模型升级。

## 可复现的依赖与语音包

- 引擎：sherpa-onnx 1.13.7，`app/libs/sherpa-onnx-1.13.7.aar`，来自 [官方发布](https://github.com/k2-fsa/sherpa-onnx/releases/download/v1.13.7/sherpa-onnx-static-link-onnxruntime-1.13.7.aar)。
- 模型：[kokoro-multi-lang-v1_0.tar.bz2](https://github.com/k2-fsa/sherpa-onnx/releases/download/tts-models/kokoro-multi-lang-v1_0.tar.bz2)。
- 取得并解压模型后，执行 `python3 scripts/package_kokoro.py /path/to/kokoro-multi-lang-v1_0`，生成 ZIP、内置资源分片与 App 资源中的文件校验清单。生成器只收录英文所需模型、双口音词典、音色、音素数据及许可文件。
- [官方声音映射](https://k2-fsa.github.io/sherpa/onnx/tts/pretrained_models/kokoro.html)。

依赖许可及来源见 `third_party/README.md`。模型更新需要重新核对声音映射、生成清单并重新构建 App。

## 验证

使用专用测试模拟器，避免覆盖用户学习数据。直接运行，无需推送外部模型：

```bash
source scripts/android-env.sh
ANDROID_SERIAL=emulator-5556 ./gradlew --offline testDebugUnitTest lintDebug assembleDebug connectedDebugAndroidTest
python3 -m unittest discover -s tests
```

设备测试会只使用 APK 内置资源自动准备语音包、分别用两种口音生成有声音的 24 kHz WAV，并确认 App 没有联网权限。单元测试覆盖 ZIP 文件缺失、损坏、路径越界和正常解压；界面测试覆盖来源/口音选择与持久化。
