# 安卓单词晨测 App V1.0 技术方案文档

> 文档状态：V1.0 实施基线
>
> 上游文档：`产品需求文档.md`、`设计文档.md`
>
> 适用范围：Android V1.0 Phase 1–6
>
> 目标：将已批准设计拆解为可编码、可迁移、可测试、可逐阶段验收的实现方案

## 1. 实施原则

1. 业务优先级依次为数据完整性、状态可恢复性、业务正确性、交互体验和视觉细节。
2. 采用单 `app` module，避免在 V1.0 引入无业务收益的多模块复杂度。
3. 业务状态以 Room 为唯一事实来源；内存状态只用于页面渲染和进行中的请求状态。
4. 核心状态机为纯 Kotlin，实现不得依赖 Compose、Context、Room Entity 或 Android 生命周期。
5. 每个 Phase 按“数据层 → 领域层 → UI → 测试 → 全量回归”顺序交付。
6. 不引入 PRD 排除项，不为账号、网络、云同步或 AI 功能预留复杂抽象。
7. 技术方案与上游文档冲突时，以产品需求文档为最高业务基线；已确认的设计补充项 `SessionBatch` 和错词复测幂等口径按本文实现。

## 2. 工程与构建方案

### 2.1 工程基线

- Kotlin Android application
- Jetpack Compose + Material 3
- Navigation Compose
- AndroidX Lifecycle ViewModel + StateFlow
- Room + KSP
- Preferences DataStore
- Kotlin Coroutines
- Android TextToSpeech
- JUnit、Kotlin coroutine test、Room instrumentation test、Compose UI test

依赖版本统一在 Gradle version catalog 中锁定，不使用 `+` 或其他动态版本。初始化工程时选择相互兼容的稳定版本并记录在 `gradle/libs.versions.toml`；升级依赖作为独立 commit，必须运行完整回归。

### 2.2 建议目录

```text
.
├── AGENTS.md
├── 产品需求文档.md
├── 设计文档.md
├── 技术方案文档.md
├── settings.gradle.kts
├── build.gradle.kts
├── gradle/
│   └── libs.versions.toml
└── app/
    ├── build.gradle.kts
    ├── schemas/
    └── src/
        ├── main/
        │   ├── AndroidManifest.xml
        │   └── java/<package>/
        │       ├── MorningWordsApplication.kt
        │       ├── data/
        │       │   ├── dao/
        │       │   ├── entity/
        │       │   ├── local/
        │       │   ├── mapper/
        │       │   ├── repository/
        │       │   └── transaction/
        │       ├── domain/
        │       │   ├── model/
        │       │   ├── repository/
        │       │   ├── statemachine/
        │       │   └── usecase/
        │       ├── navigation/
        │       ├── ui/
        │       │   ├── home/
        │       │   ├── library/
        │       │   ├── test/
        │       │   ├── wrongword/
        │       │   ├── settings/
        │       │   └── common/
        │       └── util/
        ├── test/
        └── androidTest/
```

`<package>` 在创建工程时确定，之后不在业务 Phase 中改名。Room schema 通过 `room.schemaLocation` 导出到 `app/schemas/` 并纳入 Git。

### 2.3 依赖边界

```text
Composable
  ↓ intent / immutable UiState
ViewModel
  ↓ Use Case
Domain state machine + repository contract
  ↓ implementation
Repository implementation
  ↓
Room DAO / transaction object + DataStore
```

- `domain` 不引用 `android.*`、Compose、Room annotation 或数据库实体。
- `data` 可引用 domain contract，但领域逻辑不能反向散落进 DAO。
- `ui` 不持有 DAO、Entity 或可变数据库对象。
- 时间和随机顺序通过 `Clock`、`QueueShuffler` 接口注入，测试使用确定性 fake。

## 3. 固定枚举与内部类型

### 3.1 产品固定枚举

```kotlin
enum class SessionType { DAILY_TEST, WRONG_REVIEW }
enum class TestMode { STUDENT, PARENT }
enum class SessionStatus { IN_PROGRESS, COMPLETED, ABANDONED }
enum class TestPhase { FIRST_ROUND, WRONG_LOOP, FINAL_CHECK, WRONG_REVIEW, ROUND_SUMMARY, COMPLETED }
enum class TestResult { KNOW, UNKNOWN, MASTERED }
enum class WrongWordStatus { ACTIVE, MASTERED }
enum class WrongRecordType { FIRST_ROUND_WRONG, WRONG_REVIEW_WRONG }
```

Room 使用严格的 TypeConverter 在枚举名与字符串间转换。遇到未知数据库值时抛出数据一致性错误，不静默映射到默认值。

### 3.2 内部持久化类型

```kotlin
enum class SessionWordQueueState {
    NOT_ANSWERED,
    QUEUED,
    PASSED
}
```

`NOT_ANSWERED` 表示当前 phase 尚未提交；`QUEUED` 表示当前或下一轮待作答；`PASSED` 表示当前状态机下已经通过。是否属于终验全集只由 `wasFirstRoundWrong` 表示，不从 `queueState` 反推。

### 3.3 可测试基础接口

```kotlin
fun interface Clock {
    fun nowEpochMillis(): Long
}

fun interface QueueShuffler {
    fun shuffled(wordIds: List<Long>): List<Long>
}
```

生产使用系统 UTC 时钟和 Kotlin 随机；测试使用固定时间和固定排列。

## 4. Room 实现方案

### 4.1 数据库

`AppDatabase` 声明全部 Entity、DAO 和 transaction DAO，初始 `version = 1`，`exportSchema = true`。数据库实例为 Application 级单例；不启用 destructive migration。

建议文件名：`morning_words.db`。命名属于实现细节，但一旦发布不得无迁移变更。

### 4.2 Entity 与索引

| Entity | 表名 | 主键 | 关键索引/约束 |
|---|---|---|---|
| `WordEntity` | `Word` | `id` | unique `normalizedWord` |
| `WordBatchEntity` | `WordBatch` | `id` | `createdAt` |
| `BatchWordEntity` | `BatchWord` | `id` | unique `(batchId, wordId)`；`(batchId, sortOrder)` |
| `TestSessionEntity` | `TestSession` | `id` | `status`；`updatedAt` |
| `SessionBatchEntity` | `SessionBatch` | `id` | unique `(sessionId, batchId)`；`(sessionId, selectionOrder)` |
| `SessionWordEntity` | `SessionWord` | `id` | unique `(sessionId, wordId)`；`(sessionId, queueState, queueOrder)` |
| `TestRecordEntity` | `TestRecord` | `id` | `(sessionId, createdAt)`；`(sessionId, wordId)` |
| `WrongWordEntity` | `WrongWord` | `id` | unique `wordId`；`(status, lastWrongAt)` |
| `WrongRecordEntity` | `WrongRecord` | `id` | unique `(testSessionId, wordId, recordType)`；`(recordType, wrongAt, wordId)` |
| `UndoSnapshotEntity` | `UndoSnapshot` | `sessionId` | one row per Session |

所有 `id` 使用 `Long`。所有时间使用 UTC epoch milliseconds。可空文本在 mapper 中将纯空白转成 `null`，英文展示字段 `word` 例外：trim 后不得为空。

### 4.3 外键策略

- `BatchWord.batchId → WordBatch.id`：`CASCADE`，因为它只是批次关联。
- `BatchWord.wordId → Word.id`：`NO ACTION`。
- `SessionBatch.sessionId → TestSession.id`：`NO ACTION`。
- `SessionBatch.batchId → WordBatch.id`：`NO ACTION`，批次删除事务先检查状态，再显式删除已完成/已放弃 Session 的来源关联。
- `SessionWord`、`TestRecord`、`WrongRecord` 对 Session/Word 的外键均为 `NO ACTION`。
- `WrongWord.wordId → Word.id`：`NO ACTION`。
- `UndoSnapshot.sessionId → TestSession.id`：`NO ACTION`。

V1.0 不提供删除历史 Session 的 API。Word 不做后台孤儿清理。

### 4.4 `UndoSnapshot` 编码

为避免建立大量仅服务一次撤销的明细表，`UndoSnapshot` 使用以下字段：

- `sessionId`
- `testRecordId`
- `wrongRecordId: Long?`
- `wrongRecordCreated: Boolean`
- `sessionBeforeJson`
- `sessionWordsBeforeJson`
- `wrongWordBeforeJson: String?`
- `wrongWordDidNotExist: Boolean`
- `createdAt`

JSON 只存内部、版本明确的最小快照 DTO，不直接序列化 Room Entity。快照 DTO 含 `snapshotVersion = 1`；解码失败视为恢复校验失败。项目若无现成 JSON 方案，使用 Android 平台能力实现最小编码，新增序列化库必须先单独说明必要性。

### 4.5 Migration 规则

从数据库发布后的每个版本到下一版本都提供：

1. `Migration(old, new)`；
2. 更新后的 `app/schemas/<version>.json`；
3. migration instrumentation test；
4. 升级前后关键数据断言；
5. 完整 DAO 回归测试。

禁止通过 `fallbackToDestructiveMigration` 解决 schema 差异。

## 5. DAO 与查询契约

### 5.1 基础 DAO

`WordDao`：

- `findByNormalizedWord(normalizedWord)`
- `insert(word)`，冲突时返回失败，由 Repository 决定查询复用
- `updateContent(...)`
- `getWordsByIds(ids)`

`BatchDao`：

- `observeBatchesWithWordCount()`
- `getBatch(batchId)`
- `getBatchWords(batchId)`
- `insertBatch(...)`
- `insertBatchWords(...)`
- `updateBatchMetadata(...)`
- `deleteBatchWords(batchId)` / `deleteBatch(batchId)`

`SessionDao`：

- `getInProgressSession()`
- `getSession(sessionId)`
- `insertSession(...)`
- `updateSession(...)`
- `insertSessionBatches(...)`
- `getSessionWords(sessionId)`
- `getCurrentQueuedWord(sessionId, queueOrder)`
- `updateSessionWords(...)`
- `insertTestRecord(...)`

`WrongWordDao`：

- `findByWordId(wordId)`
- `insertWrongWord(...)`
- `updateWrongWord(...)`
- `observeActiveSummary(...)`
- `queryActiveWrongWords(sort, filter)`
- `queryWrongHistory(wordId)`
- `queryReviewWordIds(startInclusive, endInclusive)`

`UndoDao`：

- `getSnapshot(sessionId)`
- `replaceSnapshot(snapshot)`
- `deleteSnapshot(sessionId)`

### 5.2 事务 DAO

定义 `MorningWordsTransactionDao`，其公开方法均使用 `@Transaction`，内部组合基础 DAO：

```kotlin
suspend fun importBatch(command: ImportBatchCommand): Long
suspend fun createDailySession(command: CreateDailySessionCommand): Long
suspend fun createWrongReviewSession(command: CreateWrongReviewSessionCommand): Long
suspend fun submitAnswer(command: SubmitAnswerCommand): SessionState
suspend fun undoLastAnswer(sessionId: Long): SessionState
suspend fun abandonSession(sessionId: Long)
suspend fun deleteBatchSafely(batchId: Long): BatchDeleteResult
```

调用方不能用多个 DAO 调用自行拼接这些业务操作。

### 5.3 错词时间范围查询

```sql
SELECT wr.wordId
FROM WrongRecord AS wr
JOIN WrongWord AS ww ON ww.wordId = wr.wordId
WHERE wr.recordType = 'FIRST_ROUND_WRONG'
  AND wr.wrongAt BETWEEN :startInclusive AND :endInclusive
  AND ww.status = 'ACTIVE'
GROUP BY wr.wordId
ORDER BY MAX(wr.wrongAt) DESC
```

自定义日期由 domain 把设备时区中的开始日 `00:00:00.000` 和结束日次日零点前一毫秒转换为 UTC。DAO 只接受已计算的闭区间 epoch milliseconds。

## 6. Repository 契约

领域层定义接口，数据层实现：

```kotlin
interface WordLibraryRepository {
    suspend fun previewImport(input: ImportInput): ImportPreview
    suspend fun confirmImport(command: ConfirmImportCommand): Result<Long>
    fun observeBatches(): Flow<List<WordBatchSummary>>
    suspend fun deleteBatch(batchId: Long): BatchDeleteResult
}

interface TestSessionRepository {
    suspend fun createDailySession(command: CreateDailySession): CreateSessionResult
    suspend fun createWrongReviewSession(command: CreateWrongReviewSession): CreateSessionResult
    suspend fun loadActiveSession(): LoadSessionResult
    suspend fun loadSession(sessionId: Long): LoadSessionResult
    suspend fun submitAnswer(command: SubmitAnswer): SubmitAnswerResult
    suspend fun undo(sessionId: Long): UndoResult
    suspend fun abandon(sessionId: Long): Result<Unit>
}

interface WrongWordRepository {
    fun observeSummary(): Flow<WrongWordSummary>
    fun observeActiveWords(query: WrongWordQuery): Flow<List<WrongWordItem>>
    suspend fun getDetails(wordId: Long): WrongWordDetails
}
```

接口返回 sealed result，而不是把 SQLite/Room 异常直接暴露给 ViewModel。基础设施异常映射为可重试错误；数据不一致映射为必须提示放弃或重试恢复的错误。

## 7. 纯 Kotlin 状态机

### 7.1 输入与输出

```kotlin
data class AnswerInput(
    val session: SessionSnapshot,
    val currentWord: SessionWordSnapshot,
    val result: TestResult,
    val answeredAt: Long
)

data class TransitionPlan(
    val testRecord: TestRecordDraft,
    val sessionMutation: SessionMutation,
    val wordMutations: List<SessionWordMutation>,
    val wrongWordMutation: WrongWordMutation?,
    val wrongRecordDraft: WrongRecordDraft?,
    val nextQueue: List<Long>,
    val completed: Boolean
)
```

状态机只计算 `TransitionPlan`，不写数据库。事务层先持久化 UndoSnapshot，再按 plan 执行写入。

### 7.2 合法性检查

统一在状态转换前拒绝：

- Session 不是 `IN_PROGRESS`；
- 当前 wordId 与持久化队列当前位置不一致；
- `DAILY_TEST` 使用 `MASTERED`；
- `WRONG_REVIEW` 进入 `FIRST_ROUND` 或兼容阶段 `FINAL_CHECK`；
- phase 为 `COMPLETED` 仍提交；
- 相同当前词已经存在本次已提交状态；
- 队列或统计不满足恢复一致性校验。

### 7.3 `FIRST_ROUND` 算法

```text
result == KNOW:
  firstPassCount += 1
  current queueState = PASSED

result == UNKNOWN:
  firstWrongCount += 1
  current wasFirstRoundWrong = true
  current queueState = QUEUED
  upsert WrongWord: wrongCount + 1, ACTIVE, update lastWrongAt
  create FIRST_ROUND_WRONG

if 仍有首轮未作答词:
  前进到下一个 displayOrder
else:
  phase = ROUND_SUMMARY
```

最后一个首轮答案的判断使用“提交后的预计统计”，不能误用事务前 `firstWrongCount`。

### 7.4 `WRONG_LOOP` 算法

```text
KNOW    → current PASSED
UNKNOWN → current QUEUED for next round

if 当前轮仍有未作答词:
  前进
else:
  phase = ROUND_SUMMARY
```

此阶段不生成 WrongRecord、不改变 `wrongCount` 或 `reviewWrongCount`。

### 7.5 `ROUND_SUMMARY` 与按需复测算法

统计完全由当前 `roundNumber` 的 `SessionWord.lastResult` 派生：

```text
roundTestedCount = 当前轮已有结果的词数
roundKnownCount = 当前轮 KNOW 或 MASTERED 的词数
roundWrongCount = 当前轮 UNKNOWN 的词数

点击“错题重新测试”且 roundWrongCount > 0:
  phase = WRONG_LOOP
  roundNumber += 1
  只将本轮 UNKNOWN 设为待测试

点击“退出并完成今日测试”:
  status/phase = COMPLETED
```

`FINAL_CHECK` 枚举只为读取旧 Session 保留；新流程不会生成该阶段。

### 7.6 `WRONG_REVIEW` 算法

```text
KNOW:
  current PASSED
  WrongWord 保持 ACTIVE

MASTERED:
  current PASSED
  WrongWord = MASTERED, masteredAt = answeredAt

UNKNOWN:
  current QUEUED
  reviewWrongCount += 1
  WrongWord = ACTIVE
  ensure WRONG_REVIEW_WRONG exists for session/word/type

if 当前轮仍有未作答词:
  前进
else:
  phase = ROUND_SUMMARY
```

每次答错均由 `TestRecord` 审计并增加 `reviewWrongCount`。根据已批准设计，`WrongRecord` 遵守 `(testSessionId, wordId, recordType)` 唯一索引，同一复测 Session 的重复轮次执行 ensure-exists，不创建第二条。

## 8. 原子事务流程

### 8.1 创建正常晨测

1. 查询 `IN_PROGRESS` Session；存在则返回 `AlreadyInProgress(sessionId)`。
2. 校验选择批次非空。
3. 按 command 中用户选择的批次顺序，再按 `BatchWord.sortOrder` 查询候选词。
4. 按 `wordId` 保序去重，取第一个非空 `requiredMeaning`。
5. 去重结果为空则返回 `NoWordsSelected`。
6. 插入 `TestSession`、`SessionBatch`、`SessionWord`。
7. 返回新 Session ID。

上述步骤处于同一事务。`totalCount` 等于写入的唯一 SessionWord 数。

### 8.2 提交作答

1. 重读 Session、当前词、当前队列和最近 UndoSnapshot。
2. 执行恢复一致性和动作合法性检查。
3. 由状态机生成 `TransitionPlan`。
4. 创建包含所有受影响行前值的 UndoSnapshot，并覆盖旧快照。
5. 插入 `TestRecord`。
6. 若需要，更新/插入 `WrongWord` 和确保 `WrongRecord` 存在。
7. 更新全部受影响 `SessionWord`。
8. 更新 `TestSession` 统计、phase、round、index、status 和时间。
9. 返回事务后 SessionState。

任何一步失败均回滚；UI 不前进。

### 8.3 撤销

1. 读取当前 Session 的 UndoSnapshot。
2. 校验 `testRecordId` 是该 Session 最新作答记录。
3. 删除该 TestRecord。
4. 仅当 `wrongRecordCreated=true` 时删除该 WrongRecord；ensure-exists 的既有记录不得误删。
5. `wrongWordDidNotExist=true` 时删除本次新建 WrongWord，否则恢复完整前值。
6. 恢复 TestSession 和所有 SessionWord 前值。
7. 删除 UndoSnapshot。
8. 返回恢复后的 SessionState。

必须覆盖 `FIRST_ROUND → WRONG_LOOP`、`WRONG_LOOP → FINAL_CHECK` 和 `FINAL_CHECK → COMPLETED` 的反向恢复。

### 8.4 安全删除批次

1. 通过 `SessionBatch` 查询是否被 `IN_PROGRESS` Session 直接引用。
2. 若引用，返回 `BlockedByInProgressSession(sessionId)`，不写任何数据。
3. 删除已完成/已放弃 Session 的相关 `SessionBatch` 来源行。
4. 删除该批次的 `BatchWord`。
5. 删除 `WordBatch`。
6. 不删除 Word、SessionWord、TestRecord、WrongWord 或 WrongRecord。

### 8.5 放弃 Session

事务内将 Session 设为 `ABANDONED`、更新 `updatedAt`、清除 UndoSnapshot。所有测试和错误历史保留。事务成功后才允许创建新 Session。

## 9. 导入实现

### 9.1 输入模型

```kotlin
sealed interface ImportInput {
    data class PlainText(val content: String) : ImportInput
    data class MeaningText(val content: String) : ImportInput
    data class Csv(val bytes: ByteArray, val declaredCharset: String?) : ImportInput
}
```

解析输出 `ImportPreview`，每行包含 `lineNumber`、`rawText`、解析字段、`normalizedWord`、`VALID / DUPLICATE / NEEDS_REVIEW / ERROR` 和可理解原因。

### 9.2 纯文本与带释义文本

- trim 空行后忽略空行，但预览保留行号。
- 第一段可靠识别为英文单词时写入 `word`。
- 后续内容解析为词性、`meaning` / `requiredMeaning` 和 `example`；词性支持 `n./vt.`、`vt./vi.` 等斜杠组合形式。当释义段包含中文且后续存在完整英文句子时，以语义边界将两者拆分。
- 无法可靠拆分时不丢弃，保存 `rawText` 并标为 `NEEDS_REVIEW`。
- 启动后执行幂等修复事务：按 `BatchWord.id` 保序读取 `rawText`，补全 `Word.partOfSpeech` / `Word.example`，并在旧 `meaning` 确实包含解析出的例句时替换为纯释义；同时刷新 `BatchWord.requiredMeaning`。
- 批内按 `normalizedWord` 保留第一条，后续标为重复。

### 9.3 CSV

- 先检测并解码 UTF-8；若有明确 BOM 则按 BOM；无法识别时阻止提交。
- 表头必须含 `word`，其余支持字段可缺失或为空。
- 正确处理带引号字段、字段内逗号、双引号转义和字段内换行。
- 解析预览阶段不写数据库。
- 确认后一次事务写入；冲突 Word 复用现有 ID，同一批次不重复创建 BatchWord。

### 9.4 Word 内容合并

跨批次命中既有 Word 时：

- 不用空值覆盖已有非空字段。
- 新值非空、旧值为空时补齐全局 Word 内容。
- 新旧非空且不同，V1.0 保留已有全局内容；本课词义由 `BatchWord.requiredMeaning` 保存。
- 展示拼写保留首次规范拼写，规范键始终按 `trim().lowercase(Locale.ROOT)`。

## 10. ViewModel 与 UI 实现

### 10.1 ViewModel 规范

每个页面 ViewModel 公开单一不可变 `StateFlow<UiState>`，用户动作通过显式 intent 方法进入。写操作使用互斥单飞；`isSubmitting=true` 时忽略重复答案动作。

ViewModel 不保存可恢复队列的唯一副本。页面首次进入和 process recreation 后均从 Repository 读取。

### 10.2 页面与 ViewModel

| 页面域 | 主要 ViewModel | 关键状态/动作 |
|---|---|---|
| 今日 | `HomeViewModel` | 概览、开始晨测、继续晨测 |
| 批次 | `BatchListViewModel`, `BatchDetailViewModel` | 列表、选择、编辑、安全删除 |
| 导入 | `ImportViewModel` | 输入、预览、确认、错误行 |
| 创建测试 | `SessionSetupViewModel` | 批次多选、去重词数、模式 |
| 测试 | `TestViewModel` | 当前卡片、答案可见性、作答、撤销、恢复 |
| 完成 | `CompletionViewModel` | 首轮与最终统计 |
| 错词 | `WrongWordViewModel` | 概览、筛选、排序、详情 |
| 错词复测 | `WrongReviewSetupViewModel`, `TestViewModel` | 时间范围、三按钮、循环 |
| 设置 | `SettingsViewModel` | DataStore、TTS 偏好、导出 |

### 10.3 测试页状态

```kotlin
sealed interface TestUiState {
    data object Loading : TestUiState
    data class Ready(
        val card: WordCardUiModel,
        val progress: ProgressUiModel,
        val phase: TestPhase,
        val mode: TestMode,
        val answerVisible: Boolean,
        val isSubmitting: Boolean,
        val canUndo: Boolean,
        val ttsAvailable: Boolean
    ) : TestUiState
    data class RecoverFailure(val message: String) : TestUiState
    data class Completed(val sessionId: Long) : TestUiState
}
```

学生模式作答前隐藏中文、词性和例句；家长模式直接显示完整答案。学生首次点击“会”只将 `answerVisible` 设为 `true`并保留当前卡片，不调用 Repository；随后点击“我错了”才提交 `UNKNOWN`，点击“我对了”才提交 `KNOW`。两种最终选择都在事务成功后清除答案卡并进入下一词或轮次统计。直接点击“不会”仍立即提交 `UNKNOWN`；家长模式继续在首次点击时直接提交。

例句统一通过 `highlightedExample(example, word)` 生成 `AnnotatedString`，对完整单词边界内的原词及常见词形追加红色粗体 `SpanStyle`，供测试卡和错词详情复用。

固定文案：`会`、`不会`、`会（移出错词库）`、`继续晨测`、`今日完成`。

### 10.4 Navigation routes

```text
home
library
batch/{batchId}
batch/import
batch/new
batch/{batchId}/edit
test/batches
test/mode
test/session/{sessionId}
test/session/{sessionId}/completed
wrong
wrong/list
wrong/{wordId}
wrong/review/range
wrong/review/{sessionId}
settings
```

route 只传 ID。底部导航仅在四个一级页面展示，测试和编辑流程隐藏底栏以减少误触。

`library` 中的词库卡片点击后进入 `batch/{batchId}`。ViewModel 使用 `wordsInBatch(batchId)` 异步加载，DAO 以 `BatchWord.sortOrder` 排序返回单词。页面用单一 `expandedWordIds` 集合管理展开状态，展开项显示词性、完整释义和带目标词高亮的例句。

## 11. 设置、TTS 与导出

### 11.1 DataStore

Preferences keys：

```text
defaultTestMode = STUDENT
autoPronounce = true
voiceAccent = US
showAnswerAfterKnow = true
fontSize = STANDARD
```

DataStore repository 返回 `Flow<AppSettings>` 并对未知值回退到产品默认设置；这种回退只适用于设置，不适用于 Room 业务枚举。

### 11.2 TextToSpeech

实现 `TtsController`：

- 由测试页面生命周期创建和释放，不放入 Room 或 DataStore repository。
- `speak(word, cardId)` 使用 cardId 去重自动播放，Compose 重组不重复触发。
- US/UK locale 不可用时回退可用英文 locale。
- 初始化、缺少语言包或播放失败映射为 `Unavailable`，只禁用发音按钮并显示非阻塞提示。
- fake controller 覆盖自动播放、手动重播、失败和释放测试。

### 11.3 本地导出

Phase 6 实现 JSON 导出：

```text
exportVersion
schemaVersion
appVersion
exportedAt
tables { ...全部业务表... }
settings { ... }
```

先在 app cache 写临时文件，完整编码并重新读取校验后，通过 Storage Access Framework 让用户选择保存位置。V1.0 若未实现完整恢复，只展示“导出本地数据”，不使用“可恢复备份”文案。

## 12. 错误模型与可恢复行为

领域错误至少包括：

```kotlin
sealed interface DomainError {
    data object NoWordsSelected : DomainError
    data class SessionAlreadyInProgress(val sessionId: Long) : DomainError
    data class BatchReferencedByActiveSession(val sessionId: Long) : DomainError
    data object InvalidAnswerForSessionType : DomainError
    data object StaleAnswerSubmission : DomainError
    data object SessionStateInconsistent : DomainError
    data object StorageFailure : DomainError
}
```

行为映射：

| 错误 | UI 行为 |
|---|---|
| 无词或空筛选 | 不创建 Session，提示先导入/调整范围 |
| 已有进行中 Session | 导航至“继续晨测”，不覆盖 |
| 批次被进行中任务引用 | 阻止删除并说明先完成或放弃 |
| 重复/过期提交 | 保持当前页并重新加载数据库状态 |
| 数据库写入失败 | 不前进，提示重试 |
| 恢复一致性失败 | 保留数据，提示重试或明确放弃 |
| TTS 失败 | 仅禁用发音，测试继续 |
| CSV 结构/编码错误 | 阻止确认，定位错误原因 |

日志不记录完整单词库或导出内容。V1.0 不接入网络日志服务。

## 13. 测试实施方案

### 13.1 纯 Kotlin 单元测试

位于 `app/src/test`，使用 fixed Clock 和 deterministic QueueShuffler：

- `FirstRoundTest`
- `WrongLoopTest`
- `FinalCheckTest`
- `WrongWordInsertTest`
- `WrongWordDuplicateTest`
- `WrongWordReactivateTest`
- `WrongReviewTest`
- `ImportNormalizationTest`
- 日期范围边界测试
- 统计口径测试

状态机必须逐步断言 phase、round、queue、统计和长期错词 mutation，而不只断言最终完成。

### 13.2 Room instrumentation test

位于 `app/src/androidTest`，使用 in-memory Room：

- `TestSessionRestoreTest`
- `UndoTest`
- `TransactionFailureTest`
- 唯一索引与重复点击测试
- `SessionBatch` 删除阻止与完成后删除测试
- WrongRecord ensure-exists 与 `reviewWrongCount` 多轮累计测试
- Migration test

故障注入通过 transaction collaborator/fake DAO seam 在指定写入步骤抛错，断言所有表保持事务前状态。

### 13.3 ViewModel 与 Compose 测试

- 两种模式答案可见性。
- “不会”后答案展示与“继续”行为。
- 提交期间按钮禁用和重复 intent 忽略。
- 完成页统计。
- “会（移出错词库）”只在错词复测出现。
- TTS 不可用不阻断流程。
- 恢复失败文案及放弃入口。

### 13.4 AC 追踪

| AC | 实施位置 | 自动化验证 |
|---|---|---|
| AC-01 | 7.3、8.2 | `FirstRoundTest`, `WrongWordInsertTest` |
| AC-02 | 7.4 | `WrongLoopTest`, `FinalCheckTest` |
| AC-03 | 7.5 | `FinalCheckTest` |
| AC-04 | 7.5、10.3 | `FinalCheckTest`, 完成页测试 |
| AC-05 | 4.2、8.2 | `WrongWordDuplicateTest` |
| AC-06 | 7.6 | `WrongReviewTest` |
| AC-07 | 7.3 | `WrongWordReactivateTest` |
| AC-08 | 4、8 | `TestSessionRestoreTest` |
| AC-09 | 4.3、8.4 | 安全删除 instrumentation test |
| AC-10 | 8.1 | Session 创建去重测试 |
| AC-11 | 7.3 | `FirstRoundTest` |
| AC-12 | 8.3 | `UndoTest` |
| AC-13 | 11.2 | TTS controller / UI test |
| AC-14 | 9 | 导入解析与 `TransactionFailureTest` |

### 13.5 标准验证命令

项目初始化后，每个 Phase 至少运行：

```bash
./gradlew testDebugUnitTest
./gradlew lintDebug
./gradlew assembleDebug
./gradlew connectedDebugAndroidTest
```

无可用模拟器/设备时，`connectedDebugAndroidTest` 不能伪报通过；交付摘要必须明确标为未运行，并在 Phase 完成前补跑。文档本身的仓库级测试运行：

```bash
python3 -m unittest discover -s tests -v
git diff --check
```

## 14. Phase 实施清单

### Phase 1：词库、导入和批次

新增：工程骨架、Word/WordBatch/BatchWord Entity 和 DAO、Repository、文本解析、导入预览与确认、批次页面。

出口：规范化、批内/批间去重、事务失败、批次计数和列表测试通过。

### Phase 2：Session 与首轮

新增：TestSession、SessionBatch、SessionWord、TestRecord、固定枚举、Session 创建、`FIRST_ROUND` 状态机、两种模式测试页。

出口：首轮零错、会/不会、统计、组合批次去重和进行中 Session 防覆盖通过。

### Phase 3：循环、终验和恢复

新增：`WRONG_LOOP`、`FINAL_CHECK`、完成页、队列持久化、恢复校验。

出口：三条规定状态路径、进程恢复和完整终验测试通过。

### Phase 4：长期错词

新增：WrongWord、WrongRecord、首轮错词事务、列表、详情和历史。

出口：唯一 WrongWord、跨 Session 累计、历史保留和重新激活通过。

### Phase 5：错词复测

新增：时间范围、`WRONG_REVIEW` 状态机、三按钮、循环与 mastered 状态。

出口：边界日期、三按钮、多轮 UNKNOWN 计数和无终验流程通过。

### Phase 6：撤销及完善

新增：UndoSnapshot、撤销事务、TTS、DataStore 设置、CSV、导出、异常 UI、整体视觉完善。

出口：Undo 原子性、阶段反向恢复、TTS 失败、CSV、导出校验、全部 AC 和完整回归通过。

## 15. Commit 与交付规则

每个可审查改动创建独立 Git commit，建议粒度：

```text
build: initialize Android project
feat: add word library persistence
feat: add import preview and batch transaction
feat: add first round state machine
test: cover final check transitions
```

每次交付只报告：修改文件、已完成功能、测试结果、遗留问题。测试失败或规定验证未运行时不得宣称对应 Phase 完成。

## 16. 已确认决策与实施注意

1. 增加 `SessionBatch` 保存用户实际选择的批次，避免共享 Word 导致删除引用误判。
2. 正常晨测只有 `FIRST_ROUND + UNKNOWN` 增加 `wrongCount`。
3. 同一复测 Session 中同词多轮 `UNKNOWN`：每次写 TestRecord、每次增加 `reviewWrongCount`，WrongRecord 按唯一键确保存在。
4. `wasFirstRoundWrong` 是终验全集的唯一事实来源，任何循环都不能清除。
5. UndoSnapshot 在完成 Snackbar 仍可操作期间保留；窗口关闭或离开完成页后清除。
6. 已完成/放弃 Session 的批次来源关联可在删除批次时移除，但测试和错误审计链必须保留。
7. 不使用 destructive migration、Room `REPLACE` 或多事务拼接作答流程。
8. Android 工程尚未创建前，本文件不锁定具体库版本号；初始化时固定稳定兼容版本并由 version catalog 管理。

## 17. Definition of Done

一个功能或 Phase 只有同时满足以下条件才完成：

- 实现符合 PRD、设计文档和本文；
- 业务逻辑不位于 Composable；
- 新增/更新相关单元测试和 instrumentation test；
- 新增 schema 变更具备 migration、schema 导出和迁移测试；
- 单元测试、lint、构建、设备测试及已有回归全部通过；
- 数据库失败不产生部分写入；
- 退出或进程终止后可恢复最近一次成功状态；
- 固定枚举和 UI 文案未漂移；
- 无历史数据被批次删除或 mastered 操作物理删除；
- 已创建对应 Git commit，并在交付摘要中记录测试结果和遗留问题。
