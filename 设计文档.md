# 安卓单词晨测 App V1.0 技术设计文档

> 文档状态：V1.0 设计基线
>
> 需求来源：`产品需求文档.md`
>
> 目标平台：Android
>
> 设计原则：离线优先、事务一致、状态可恢复、业务规则可独立测试

## 1. 文档目的与范围

本文将产品需求转换为可实施、可验证的 Android 技术方案，覆盖 V1.0 的模块边界、数据模型、状态机、事务、恢复、导航、异常处理和测试策略。

V1.0 使用 Kotlin、Jetpack Compose、Material 3、Navigation Compose、ViewModel、StateFlow、Room、Repository Pattern、DataStore 和 Android TextToSpeech。应用为本地单机应用，不包含账号、服务器、云同步、网络词典、AI 学习计划或 PRD 中其他排除项。

本文不改变 PRD 的固定枚举、表名、字段语义和验收口径；若两份文档出现冲突，以产品需求文档为准。

## 2. 质量目标与核心不变量

### 2.1 质量目标

1. 每次作答、撤销和阶段切换均原子持久化。
2. App 退出、锁屏、后台或进程终止后，可从最近一次成功事务恢复。
3. 核心状态机可在无 Android UI、无 Compose 环境中运行单元测试。
4. 历史测试和错误记录不可因删除批次或移出错词库而丢失。
5. 数据库约束与事务逻辑共同抵御重复点击、重复导入和重复错误记录。

### 2.2 业务不变量

- 一个 `normalizedWord` 只对应一个 `Word`。
- 一个 `wordId` 至多对应一个 `WrongWord`。
- 一个 Session 中一个 `wordId` 只对应一个 `SessionWord`。
- 正常晨测只有 `DAILY_TEST + FIRST_ROUND + UNKNOWN` 能增加 `wrongCount` 并创建 `FIRST_ROUND_WRONG`。
- 错题复测轮的 `UNKNOWN` 只产生 `TestRecord`，不改变长期错词统计。
- 错词复测的 `UNKNOWN` 只增加 `reviewWrongCount` 并创建 `WRONG_REVIEW_WRONG`。
- 每轮结束必须进入 `ROUND_SUMMARY`，未经用户点击不得自动开始下一轮。
- Session 的 `totalCount` 创建后不变。
- `MASTERED` 只改变长期错词的当前状态，不删除任何历史。
- UI 仅在数据库事务成功后切换到下一张卡片。

## 3. 总体架构

### 3.1 分层结构

```text
app/
├── data/
│   ├── local/
│   │   ├── AppDatabase.kt
│   │   ├── converter/
│   │   └── transaction/
│   ├── entity/
│   ├── dao/
│   └── repository/
├── domain/
│   ├── model/
│   └── usecase/
├── ui/
│   ├── home/
│   ├── library/
│   ├── test/
│   ├── wrongword/
│   └── settings/
├── navigation/
└── util/
```

依赖方向为 `UI → domain → repository contract → data implementation`。UI 不直接访问 DAO；Repository 是 domain 和 UI 访问 Room 的唯一入口。DataStore 只存设置，不存测试进度或业务事实。

### 3.2 组件职责

| 组件 | 职责 | 禁止事项 |
|---|---|---|
| Composable | 渲染不可变状态、发送用户意图、处理短暂展示效果 | 状态机判断、队列修改、直接写库 |
| ViewModel | 编排 Use Case、公开 `StateFlow<UiState>`、处理加载与提交状态 | 保存业务事实到内存作为唯一来源 |
| Use Case / State Machine | 计算下一状态、校验动作合法性、生成事务命令 | 依赖 Compose 或 Android 页面生命周期 |
| Repository | 聚合查询、开启 Room 事务、映射实体与领域模型 | 向 UI 暴露 DAO 或可变实体 |
| DAO / Transaction | 执行约束明确的查询与原子写入 | 在多个无事务调用间维护业务一致性 |
| DataStore | 保存模式、TTS、答案展示和字体设置 | 保存 Session 队列或统计 |

### 3.3 建议的领域用例

- `PreviewImportUseCase`
- `ConfirmImportUseCase`
- `CreateDailySessionUseCase`
- `CreateWrongReviewSessionUseCase`
- `LoadActiveSessionUseCase`
- `SubmitAnswerUseCase`
- `UndoLastAnswerUseCase`
- `AbandonSessionUseCase`
- `DeleteBatchUseCase`
- `QueryWrongWordsUseCase`
- `ExportLocalDataUseCase`

`SubmitAnswerUseCase` 负责动作合法性和状态转换，最终将完整事务命令交给 Repository；Repository 不重新推导一套不同的状态机。

## 4. 固定类型与领域模型

代码、Room converter、测试夹具和日志均使用以下固定值，不创建同义枚举：

```kotlin
enum class SessionType { DAILY_TEST, WRONG_REVIEW }
enum class TestMode { STUDENT, PARENT }
enum class SessionStatus { IN_PROGRESS, COMPLETED, ABANDONED }
enum class TestPhase { FIRST_ROUND, WRONG_LOOP, FINAL_CHECK, WRONG_REVIEW, ROUND_SUMMARY, COMPLETED }
enum class TestResult { KNOW, UNKNOWN, MASTERED }
enum class WrongWordStatus { ACTIVE, MASTERED }
enum class WrongRecordType { FIRST_ROUND_WRONG, WRONG_REVIEW_WRONG }
```

内部可增加用于持久化队列状态的 `SessionWordQueueState`，它不是产品枚举的同义替代：

```kotlin
enum class SessionWordQueueState {
    NOT_ANSWERED,
    QUEUED,
    PASSED
}
```

约束：

- `MASTERED` 作答只允许 `SessionType.WRONG_REVIEW`。
- `FINAL_CHECK` 仅为兼容旧 Session 保留，新建流程不再进入该阶段。
- `WRONG_REVIEW` phase 只允许 `SessionType.WRONG_REVIEW`。
- `ROUND_SUMMARY` 不可作答但仍为 `IN_PROGRESS`；只有用户点击退出后才进入 `COMPLETED`。

## 5. Room 数据设计

### 5.1 表与索引

#### `Word`

| 字段 | 类型建议 | 约束 |
|---|---|---|
| `id` | `Long` | PK，自增 |
| `word` | `String` | 非空 |
| `normalizedWord` | `String` | 非空，唯一索引 |
| `phonetic` | `String?` | 可空 |
| `partOfSpeech` | `String?` | 可空 |
| `meaning` | `String?` | 可空 |
| `example` | `String?` | 可空 |
| `createdAt`, `updatedAt` | `Long` | UTC epoch milliseconds |

规范化函数为唯一入口：

```kotlin
fun normalizeWord(value: String): String =
    value.trim().lowercase(Locale.ROOT)
```

#### `WordBatch`

字段为 `id`、`batchName`、`createdAt`、`studyDate`、`source`、`remark`。批次允许同名，以 ID 区分。`wordCount` 始终通过 `BatchWord` 聚合，不落为独立事实字段。

#### `BatchWord`

字段为 `id`、`batchId`、`wordId`、`requiredMeaning`、`sortOrder`、`rawText`。建立 `(batchId, wordId)` 唯一索引，以及按 `batchId, sortOrder` 查询的普通索引。

#### `TestSession`

字段为 `id`、`sessionType`、`testMode`、`status`、`phase`、`createdAt`、`updatedAt`、`completedAt`、`totalCount`、`firstPassCount`、`firstWrongCount`、`currentIndex`、`roundNumber`。

额外建立 `status` 索引。Repository 在创建 Session 前查询 `IN_PROGRESS`，应用层禁止用新 Session 覆盖进行中任务。

#### `SessionBatch`

为了准确执行“进行中 Session 引用的批次禁止删除”，增加最小关联表，字段为 `id`、`sessionId`、`batchId`、`selectionOrder`，并建立 `(sessionId, batchId)` 唯一索引。该表只记录 Session 创建时实际选择的批次，不参与单词答案快照。

若 Session 已完成或放弃，删除批次时允许同时删除对应 `SessionBatch` 来源关联；`SessionWord`、`TestRecord` 和错误历史不受影响。这样既不会因两个批次共享同一个 Word 而误判引用，也不会阻止已完成任务使用过的批次被删除。

#### `SessionWord`

字段为 `id`、`sessionId`、`wordId`、`displayOrder`、`requiredMeaningSnapshot`、`wasFirstRoundWrong`、`queueState`、`queueOrder`、`lastAnsweredPhase`、`lastResult`。建立 `(sessionId, wordId)` 唯一索引，以及 `(sessionId, queueState, queueOrder)` 查询索引。

`requiredMeaningSnapshot` 在创建 Session 时确定：按用户选择批次的顺序，取该词第一个非空 `requiredMeaning`。之后即使批次被编辑，Session 展示与恢复结果也不改变。

#### `TestRecord`

字段为 `id`、`sessionId`、`wordId`、`phase`、`roundNumber`、`result`、`testMode`、`createdAt`。建立 `(sessionId, createdAt)` 和 `(sessionId, wordId)` 索引。每次成功提交作答创建一条。

#### `WrongWord`

字段为 `id`、`wordId`、`firstWrongAt`、`lastWrongAt`、`wrongCount`、`reviewWrongCount`、`status`、`masteredAt`。`wordId` 建唯一索引；计数均要求非负。

#### `WrongRecord`

字段为 `id`、`wordId`、`testSessionId`、`wrongAt`、`recordType`、`testMode`。建立 `(testSessionId, wordId, recordType)` 唯一索引，以及 `(recordType, wrongAt, wordId)` 范围筛选索引。

唯一索引既防止同一次晨测首轮错误因重复点击而重复入库，也使错词时间筛选可直接按错误事实执行。

#### `UndoSnapshot`

每个进行中 Session 至多一条，建议以 `sessionId` 为 PK。保存：

- 被撤销 `TestRecord` 的 ID；
- 可空 `WrongRecord` ID；
- 作答前完整 `TestSession` 可变字段；
- 受影响 `SessionWord` 的作答前快照；
- 受影响 `WrongWord` 的前值，或“作答前不存在”标记；
- 阶段结束时可能被批量重排的所有 `SessionWord` 前值。

快照只服务最近一次作答。新作答成功后覆盖，撤销成功后删除；Session 完成且撤销窗口关闭后删除。

### 5.2 关系与删除策略

```text
WordBatch 1 ── n BatchWord n ── 1 Word
WordBatch 1 ── n SessionBatch n ── 1 TestSession
TestSession 1 ── n SessionWord n ── 1 Word
TestSession 1 ── n TestRecord  n ── 1 Word
Word 1 ── 0..1 WrongWord
Word 1 ── n WrongRecord n ── 1 TestSession
TestSession 1 ── 0..1 UndoSnapshot
```

- `BatchWord` 随安全删除的 `WordBatch` 删除。
- 历史表外键不使用会级联删除历史的 `CASCADE`。
- `Word` 只在没有批次关联、Session 关联、测试记录和错词历史时才具备物理删除条件；V1.0 无需主动清理孤立 Word。
- `TestSession`、`SessionWord`、`TestRecord`、`WrongRecord` 构成审计链，V1.0 不提供删除历史 Session 的 UI。
- 删除批次前，在同一事务内通过 `SessionBatch` 确认没有 `IN_PROGRESS` Session 直接引用该批次；若引用则拒绝并返回可展示原因。

### 5.3 数据库版本与迁移

- schema 导出文件纳入版本控制。
- 每次 schema 变更提供显式 migration 和 migration instrumentation test。
- 禁止使用 destructive migration 处理用户业务数据。
- 所有时间存 UTC epoch milliseconds，日期范围在设备时区换算为 UTC 闭区间后查询。

## 6. Session 创建

### 6.1 正常晨测

在单一事务中：

1. 确认不存在 `IN_PROGRESS` Session；存在则返回该 Session，不创建新任务。
2. 校验至少选择一个批次且去重后词数大于零。
3. 按批次选择顺序和批内 `sortOrder` 收集单词，以 `wordId` 去重。
4. 为重复词取第一个非空 `requiredMeaning`，写入 `requiredMeaningSnapshot`。
5. 创建 `DAILY_TEST / IN_PROGRESS / FIRST_ROUND` 的 `TestSession`。
6. 按用户选择顺序创建 `SessionBatch` 来源关联。
7. 创建所有 `SessionWord`：`NOT_ANSWERED`，按初始展示顺序设置 `queueOrder`。

### 6.2 错词复测

筛选必须来自错误事件而非 Word 创建时间：

```sql
SELECT DISTINCT wr.wordId
FROM WrongRecord wr
JOIN WrongWord ww ON ww.wordId = wr.wordId
WHERE wr.recordType = 'FIRST_ROUND_WRONG'
  AND wr.wrongAt BETWEEN :startInclusive AND :endInclusive
  AND ww.status = 'ACTIVE'
```

筛选为空时不创建 Session。非空时创建 `WRONG_REVIEW / IN_PROGRESS / WRONG_REVIEW`，全部 `SessionWord` 初始为 `QUEUED`。

## 7. 正常晨测状态机

### 7.1 转换总览

```text
FIRST_ROUND
  └─ 全部单词作答 → ROUND_SUMMARY
                         ├─ 退出 → COMPLETED
                         └─ 错题重新测试 → WRONG_LOOP
                                                └─ 本轮全部作答 → ROUND_SUMMARY
```

### 7.2 `FIRST_ROUND`

- 当前词按 `displayOrder` 依次出现且只出现一次。
- `KNOW`：`firstPassCount + 1`，该词标记通过。
- `UNKNOWN`：`firstWrongCount + 1`，`wasFirstRoundWrong = true`，加入错词循环；更新唯一 `WrongWord` 并创建 `FIRST_ROUND_WRONG`。
- 最后一词提交后统一进入 `ROUND_SUMMARY`，展示已测试、会、错三个本轮统计。

### 7.3 `WRONG_LOOP`

- 只有用户在统计页点击“错题重新测试”才进入本阶段；每轮只包含上一轮答错词。
- `KNOW`：该词本轮通过，不进入下一轮。
- `UNKNOWN`：保留为下一轮 `QUEUED`，但不写 `WrongRecord`、不更新 `WrongWord`。
- 一轮结束统一回到 `ROUND_SUMMARY`；若仍有错题，用户可再次点击复测。

### 7.4 `ROUND_SUMMARY`

- 页面展示本轮测试总数、会的数量、错的数量。
- 有错题时显示“错题重新测试”按钮；点击后只将本轮 `UNKNOWN` 送入下一轮。
- “退出并完成今日测试”始终可用；点击后才将 Session 设为 `COMPLETED`。
- 下一轮仍有错题时重复本流程，不强制循环，也不再执行全量终验。

学生自测点击“会”后，卡片翻转展示释义和例句并保持停留；只有点击右侧与“会”同宽的“确定”按钮才进入下一词或轮次统计，连续操作无需移动手指。点击“不会”仍即时记录并自动进入下一词。

家长考我模式始终展示完整答案；点击“会”或“不会”均只记录一次并立即进入下一词，最后一词作答后直接进入本轮统计页。

### 7.5 统计口径

```text
pendingCount = totalCount - passedCount
firstAccuracy = totalCount == 0 ? 0 : firstPassCount / totalCount
finalPassedCount = totalCount，仅在 COMPLETED 时成立
```

测试页实时展示本轮 `roundTestedCount`、`roundKnownCount` 和 `roundWrongCount`；统计页使用同一持久化结果派生，不能把“已展示答案”视为通过。

## 8. 错词复测状态机

错词复测作答时使用 `WRONG_REVIEW` phase，轮末同样进入 `ROUND_SUMMARY`：

```text
WRONG_REVIEW 当前轮
  ├─ KNOW ───────────────→ 当前词通过，WrongWord 仍 ACTIVE
  ├─ MASTERED ───────────→ 当前词通过，WrongWord 设为 MASTERED
  └─ UNKNOWN ────────────→ 下一轮，reviewWrongCount + 1

轮末 → ROUND_SUMMARY
点击错题重新测试 → 只重排 UNKNOWN，roundNumber + 1
点击退出 → COMPLETED
```

按钮约束：

| UI 文案 | `TestResult` | 持久化变化 |
|---|---|---|
| 会 | `KNOW` | 只写 `TestRecord`，保持 `ACTIVE` |
| 不会 | `UNKNOWN` | 写 `TestRecord` 和 `WRONG_REVIEW_WRONG`，`reviewWrongCount + 1` |
| 会（移出错词库） | `MASTERED` | 写 `TestRecord`，状态设为 `MASTERED` 并写 `masteredAt` |

若 `MASTERED` 词以后在正常晨测 `FIRST_ROUND` 答 `UNKNOWN`，同一事务中恢复为 `ACTIVE`、清空 `masteredAt`、`wrongCount + 1` 并保留历史。

PRD 同时规定每次复测“不会”创建 `WRONG_REVIEW_WRONG`，并规定 `(testSessionId, wordId, recordType)` 唯一索引。一个词在同一复测 Session 多轮答“不会”时，两条规则会冲突。V1.0 设计以明确的唯一索引为幂等边界：每次 `UNKNOWN` 都创建 `TestRecord` 且 `reviewWrongCount + 1`，`WrongRecord` 在该 Session/word/type 下执行“确保存在”，重复轮次不插入第二条。这样每次作答仍可由 `TestRecord` 审计，同时不破坏 PRD 的唯一约束；若产品要求每轮各有一条 `WrongRecord`，需先扩展唯一键并同步修改 PRD 与迁移测试。

## 9. 作答事务设计

### 9.1 通用提交流程

`SubmitAnswerUseCase(sessionId, wordId, result)` 执行：

1. Repository 在 Room transaction 中重读 Session、当前 SessionWord 和最近记录。
2. 校验 Session 为 `IN_PROGRESS`、词为当前卡片、结果在当前 Session 类型下合法。
3. 将作答前所有可能变化的数据写入或覆盖 `UndoSnapshot`。
4. 创建 `TestRecord`。
5. 按 phase/result 更新 SessionWord、统计和长期错词数据。
6. 若当前轮结束，计算并持久化下一轮队列、phase、round 和 index。
7. 若满足完成条件，原子写入 `status=COMPLETED`、`phase=COMPLETED`、`completedAt`。
8. 事务成功后返回新的完整 `SessionState`；UI 才允许前进。

提交期间 ViewModel 将 `isSubmitting` 设为 true，禁用三个作答按钮。数据库唯一索引作为第二层幂等保护。事务失败时 UI 保持当前卡片，显示可重试错误。

### 9.2 首轮错误更新

使用 `wordId` 唯一约束执行查询后更新或插入：

- 不存在：创建 `WrongWord(wrongCount=1, reviewWrongCount=0, status=ACTIVE)`。
- 已存在：`wrongCount + 1`，更新 `lastWrongAt`，设 `ACTIVE`；若原为 `MASTERED`，清空 `masteredAt`。
- 创建唯一的 `FIRST_ROUND_WRONG`。

不得通过 `REPLACE` 实现 upsert，以免替换主键或破坏引用；使用 Room `@Upsert` 或显式查询后更新。

### 9.3 撤销事务

撤销只接受当前 Session 的最近一条 `UndoSnapshot`：

1. 校验快照对应最新 `TestRecord`。
2. 删除该 `TestRecord` 和快照记录的可空 `WrongRecord`。
3. 若作答前 `WrongWord` 不存在，删除本次新建的 `WrongWord`；否则恢复完整前值。
4. 恢复受影响的 `SessionWord` 和 `TestSession` 前值，包括 phase、round、index、统计、完成状态和队列顺序。
5. 删除 `UndoSnapshot`。

撤销可使 `COMPLETED → FINAL_CHECK`，也可使 `WRONG_LOOP → FIRST_ROUND`。因此完成状态在撤销窗口关闭前不能被视为不可逆 UI 事件。

## 10. 自动保存、恢复与放弃

### 10.1 恢复读取

App 启动或回到今日页时查询唯一 `IN_PROGRESS` Session，并在一次一致性读取中获取：

- Session 类型、模式、phase、round、index 和统计；
- 当前队列及已持久化顺序；
- 完整 `wasFirstRoundWrong` 集合；
- 当前卡片的内容和 `requiredMeaningSnapshot`；
- 最近一次可用 `UndoSnapshot`。

恢复不重新洗牌，不根据 TestRecord 临时猜测队列，也不重复展示已提交词。

### 10.2 一致性校验

至少校验：

- `totalCount` 等于 SessionWord 唯一词数；
- 当前 phase 与 Session 类型兼容；
- 当前索引能定位到唯一当前词；
- `FINAL_CHECK` 队列覆盖完整首轮错词集合；
- `COMPLETED` phase 与 status 一致；
- 所有计数非负且不超过合理边界。

失败时保留数据库，返回可恢复错误：“测试进度无法恢复，请重试或放弃本次测试”。禁止静默重建或覆盖。

### 10.3 明确放弃

用户确认放弃后，在事务中将 `status=ABANDONED`、更新时间并清除该 Session 的 `UndoSnapshot`。所有 `SessionWord`、`TestRecord`、`WrongWord` 和 `WrongRecord` 保留。只有完成放弃事务后才允许创建新 Session。

## 11. 导入与批次管理

### 11.1 两阶段导入

导入采用“纯解析预览 → 用户确认事务写入”：

1. 解码输入，识别纯文本、带释义文本或 CSV。
2. 对每行生成 `ImportRow`：原文、解析字段、规范键、状态和错误原因。
3. 标识有效行、批内重复行和错误/需检查行，不静默丢弃 `rawText`。
4. 用户确认后在一个事务中创建批次、upsert Word 并创建唯一 BatchWord。
5. 任一数据库错误回滚整个批次。

### 11.2 CSV 规则

- 唯一必填列为 `word`。
- 支持 `phonetic`、`partOfSpeech`、`meaning`、`requiredMeaning`、`example` 可空列。
- 编码不可识别、表头缺少 `word` 或 CSV 结构无效时阻止确认。
- CSV 使用可靠解析器语义处理引号、逗号和换行，不能简单按逗号 `split`。
- 不主动引入第三方依赖；优先评估项目现有能力或实现最小且有测试的 RFC 4180 子集。

### 11.3 安全删除批次

删除前 Repository 在事务内检查进行中 Session 引用。若存在引用，返回 `BatchDeleteBlockedByActiveSession`；否则删除 `BatchWord` 和 `WordBatch`，不删除历史表及 Word。批次编辑仅修改元数据，不回写已有 Session 快照。

## 12. UI、导航与状态

### 12.1 一级导航

底部导航固定为：

```text
今日 | 词库 | 错词 | 我的
```

默认进入“今日”。测试流程和编辑流程使用二级 route；测试页进行中时返回操作需提示继续保留进度或明确放弃，不直接删除 Session。

今日页无进行中任务时显示“开始晨测”，存在进行中任务时显示“继续晨测”。成功完成正常晨测后进入“今日完成”页。固定作答文案为“会”“不会”，错词复测额外使用“会（移出错词库）”。

### 12.2 Route 建议

```text
home
library
batch/{batchId}
batch/import
batch/new
batch/{batchId}/edit
test/batches
test/mode
test/session/{sessionId}
test/session/{sessionId}/completed
wrong
wrong/list
wrong/{wordId}
wrong/review/range
wrong/review/{sessionId}
settings
```

route 只传稳定 ID，不传完整对象或进程内队列。

### 12.3 测试页 UI 状态

```kotlin
sealed interface TestUiState {
    data object Loading : TestUiState
    data class Ready(
        val sessionId: Long,
        val phase: TestPhase,
        val card: WordCardUiModel,
        val progress: ProgressUiModel,
        val answerVisibility: AnswerVisibility,
        val isSubmitting: Boolean,
        val canUndo: Boolean
    ) : TestUiState
    data class RecoverFailure(val message: String) : TestUiState
    data class Completed(val sessionId: Long) : TestUiState
}
```

学生模式作答前隐藏词性、所有中文词义和例句；家长模式直接显示完整答案。“不会”后展示答案但不标记通过。答案顺序固定为词性、本课要求词义、普通词义、例句，缺失字段不渲染占位。

## 13. 设置与 TextToSpeech

DataStore keys 及默认值：

| Key | 默认值 |
|---|---|
| `defaultTestMode` | `STUDENT` |
| `autoPronounce` | `true` |
| `voiceAccent` | `US` |
| `showAnswerAfterKnow` | `true` |
| `fontSize` | `STANDARD` |

TTS 由生命周期感知的独立控制器管理：

- 新卡片 ID 变化且 `autoPronounce=true` 时播放一次；重组不重复播放。
- 发音按钮调用同一控制器重播。
- 按英音/美音选择 locale，不可用时回退任意可用英文语音。
- 初始化或播放失败只更新非阻塞 UI 状态，不影响作答。
- 页面或宿主销毁时停止并释放实例，ViewModel/Composable 不持有泄漏的 TTS 实例。

## 14. 本地导出与备份

V1.0 至少提供本地导出。导出包包含：

- `schemaVersion`、`exportedAt`、应用版本；
- 所有业务表数据；
- DataStore 设置快照；
- UTF-8 编码和稳定字段名。

导出先写临时文件，完成并校验后再交给系统文件选择器保存，避免生成半文件。若未实现经过验证的恢复流程，UI 明确标注“仅导出，不支持恢复”，不得宣称已备份可恢复。

## 15. 并发、幂等与异常处理

- ViewModel 使用单飞提交，提交期间禁用按钮。
- Repository 不信任 UI 防抖，事务内再次校验当前词和 Session 状态。
- 唯一索引保护规范词、SessionWord、WrongWord 和 WrongRecord。
- Room 写入异常完整回滚，UI 保持当前卡片。
- 空批次、零词筛选或无 ACTIVE 错词时不创建 Session。
- TTS 失败不影响业务事务。
- 日期范围包含起止边界，先按设备时区计算日边界再转换 UTC。
- 缺少词义、音标或例句不阻止导入或测试，只隐藏缺失字段。

## 16. 测试设计

### 16.1 测试分层

| 层级 | 工具建议 | 覆盖范围 |
|---|---|---|
| 纯 Kotlin 单元测试 | JUnit | 状态机、规范化、导入解析、范围换算、统计 |
| Repository/DAO 测试 | Room in-memory instrumentation | 事务、唯一索引、恢复、迁移、失败回滚 |
| ViewModel 测试 | coroutine test + fake repository | UI 状态、单飞提交、错误与重试 |
| Compose UI 测试 | Compose test | 两种模式的答案可见性、按钮、文案、导航 |
| 进程恢复场景 | instrumentation | 重建进程后队列、统计、撤销恢复 |

### 16.2 必测测试组

- `FirstRoundTest`
- `WrongLoopTest`
- `FinalCheckTest`
- `WrongWordInsertTest`
- `WrongWordDuplicateTest`
- `WrongWordReactivateTest`
- `WrongReviewTest`
- `TestSessionRestoreTest`
- `UndoTest`
- `ImportNormalizationTest`
- `TransactionFailureTest`

状态机至少覆盖：

```text
FIRST_ROUND → ROUND_SUMMARY → COMPLETED
FIRST_ROUND → ROUND_SUMMARY → WRONG_LOOP → ROUND_SUMMARY → COMPLETED
ROUND_SUMMARY → WRONG_LOOP → ROUND_SUMMARY
```

### 16.3 验收场景追踪

| 验收项 | 主要设计落点 | 主要测试 |
|---|---|---|
| AC-01 | 7.2、9.2 | `FirstRoundTest`, `WrongWordInsertTest` |
| AC-02 | 7.3、7.4 | `WrongLoopTest`, `FinalCheckTest` |
| AC-03 | 7.4 | `FinalCheckTest` |
| AC-04 | 7.4、7.5 | `FinalCheckTest` |
| AC-05 | 5.1、9.2 | `WrongWordDuplicateTest` |
| AC-06 | 8 | `WrongReviewTest` |
| AC-07 | 8、9.2 | `WrongWordReactivateTest` |
| AC-08 | 10 | `TestSessionRestoreTest` |
| AC-09 | 5.2、11.3 | DAO 删除策略 instrumentation test |
| AC-10 | 6.1 | `ImportNormalizationTest`, Session 创建测试 |
| AC-11 | 7.2 | `FirstRoundTest` |
| AC-12 | 9.3 | `UndoTest` |
| AC-13 | 13 | TTS fake/controller test、Compose UI test |
| AC-14 | 11 | `ImportNormalizationTest`, `TransactionFailureTest` |

## 17. 开发顺序与阶段出口

严格按 PRD Phase 1–6 推进：

1. Room 基础词库、文本导入、批次列表。
2. Session 数据、两种模式和 `FIRST_ROUND`。
3. `WRONG_LOOP`、`FINAL_CHECK`、完成、保存和恢复。
4. 长期错词及错误历史。
5. 时间范围筛选和错词复测。
6. TTS、撤销、设置、CSV、导出、UI 和异常处理。

每个 Phase 的出口条件：数据/业务实现完成，相关 UI 完成，新增单元测试和 instrumentation test 通过，已有回归测试通过，schema/migration 资料齐全，没有跨 Phase 预建排除功能。

## 18. 关键实现决策摘要

| 决策 | 原因 |
|---|---|
| Session 队列使用 `SessionWord` 行持久化 | 支持精确恢复、事务更新和可查询校验 |
| 所有作答通过一个事务入口 | 避免记录、队列、统计和错词部分成功 |
| `firstWrongWordIds` 映射为不可逆的 `wasFirstRoundWrong` | 保证每次完整终验不会被循环队列覆盖 |
| 使用 `SessionBatch` 保存实际选择的批次 | 准确阻止进行中任务的来源批次删除，避免共享 Word 误判 |
| 最近一次撤销使用持久化快照 | 支持进程恢复和阶段反向回滚 |
| 长期错词以唯一 `WrongWord` 聚合、`WrongRecord` 审计 | 同时满足当前状态、累计统计和历史追踪 |
| `requiredMeaning` 写入 Session 快照 | 批次组合和恢复时展示口径稳定 |
| 导入采用预览与确认两阶段 | 错误可见且事务失败不产生半批数据 |
| 路由只传 ID | 页面重建时以数据库为事实来源 |

## 19. 待实现时确认的非业务细节

以下内容不改变业务语义，可在对应 Phase 采用最小实现并记录：

- Room 主键生成策略和数据库文件名；
- 队列随机种子是否仅用于测试注入；生产只需持久化洗牌结果；
- 导出文件名、JSON 顶层封装和校验摘要格式；
- Snackbar 撤销窗口时长和 Material 3 具体视觉参数；
- CSV 解析在不新增第三方依赖前提下的最小合规实现。

任何选择都不得改变固定枚举、状态机完成条件、错误累计口径、历史保留规则或恢复保证。
